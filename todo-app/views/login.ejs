<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <link rel="stylesheet" href="./css/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body style="background: linear-gradient(to right, #FFD700, #FFA500);" class="flex items-center justify-center h-screen">
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2" style="background: linear-gradient(to right, #FFD700, #FFA500); padding: 10px; border-radius: 8px;">
            <%- include('header.ejs') %>
            <h4 class="py-4 text-white">Login as a new user</h4>
            <form action="/session" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <div class="py-3">
                    <label for="email" class="text-white">Email:</label>
                    <input type="email" name="email" class="border border-gray-300 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <label for="password" class="text-white">Password:</label>
                    <input type="password" name="password" class="border border-gray-300 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <button type="submit" class="text-white bg-green-600 hover:bg-green-800 font-medium rounded px-5 py-2">Sign in</button>
                </div>
            </form>
            <% if(messages && messages.error && messages.error.length > 0)  { %>
                <% for(var i=0; i<messages.error.length; i++) { %>
                    <li class="text-white"> <%= messages.error[i] %></li>
                <% } %>
            <% } %>
        </div>
    </div>
</body>

</html>